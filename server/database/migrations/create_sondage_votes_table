<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration {
    public function up(): void
    {
        Schema::create('sondage_salons', function (Blueprint $table) {
            $table->id();

            // salon_id : adapte selon ta structure
            // Si tu as une table salons avec id (int) :
            $table->foreignId('salon_id')->constrained('salons')->cascadeOnDelete();

            $table->string('titre')->default('Sondage du salon');
            $table->boolean('is_active')->default(true);

            $table->timestamps();

            // 1 sondage actif max par salon (simple et pratique)
            $table->unique(['salon_id', 'is_active']);
        });
    }

    public function down(): void
    {
        Schema::dropIfExists('sondage_salons');
    }
};
